{
  "name": "fake_account_detector",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile" : "pkg --out-path dist app.js",
    "compile-host" : "pkg -t host --out-path dist app.js",
    "compile-full": "pkg --out-path dist app.js && xcopy node_modules\\puppeteer\\.local-chromium dist\\puppeteer /E/H" 
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "prompt": "^1.0.0",
    "prompts": "^2.3.2",
    "puppeteer": "^3.3.0",
    "puppeteer-core": "^3.3.0"
  }
}
